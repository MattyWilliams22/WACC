# attempt to call a function that does not exist

# Output:

# Exit:
# 200

# Program:

begin
  int f(int x) is
    return x
  end

  int f(int x, int y) is
    return y
  end

  int f(int x, int y, int z, int a) is
    return a
  end

  int[] k = call f(1, 2, 3);
  println k
end